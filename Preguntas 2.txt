1. Sirve para indicarle a next que el componente de ejecuta del lado del cliente 
2. El layout puede importar las rutas de un componente hijo y mostrarla en los componentes padres 
3. Se crear una carpeta como [productid] y dentro de ella una page.tsx donde mostramos nuestro producto  
4. La librería que se suele usar para realizar las validaciones es zod.
5. Permite validar formularios usando un esquema y hace que las validaciones sean automáticas 
6. Para la protección de rutas usualmente se hace algo conocido como authContext utilizando useContext lo que hace es proteger las rutas por el lado del cliente y el mildware para el lado del servidor
7. el hoc es un componente que envuelve a otro componente y es similar a los hoks